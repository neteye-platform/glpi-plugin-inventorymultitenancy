name: Continuous integration

on:
  pull_request:
    branches:
      - "**"

permissions:
  contents: read

jobs:
  generate-ci-matrix:
    name: Generate CI matrix
    uses: glpi-project/plugin-ci-workflows/.github/workflows/generate-ci-matrix.yml@559ff49a6b1aa1257e02541b49495fe0c1006780 # v1.28.0
    with:
      glpi-version: "11.0.x"
  ci:
    name: "GLPI ${{ matrix.glpi-version }} - php:${{ matrix.php-version }} - ${{ matrix.db-image }}"
    needs: generate-ci-matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-ci-matrix.outputs.matrix) }}
    # TODO: Use the official GLPI action when they pin all the action SHA
    uses: ./.github/workflows/continuous-integration.yaml
    with:
      plugin-key: inventorymultitenancy
      glpi-version: ${{ matrix.glpi-version }}
      php-version: ${{ matrix.php-version }}
      db-image: ${{ matrix.db-image }}
